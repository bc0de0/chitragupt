apiVersion: v1
kind: ServiceAccount
metadata:
  name: chitragupt
  namespace: chitragupt
  annotations:
    # GCP: populated by overlay — iam.gke.io/gcp-service-account: SA@PROJECT.iam.gserviceaccount.com
    # AWS: populated by overlay — eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT:role/ROLE
    # Azure: populated by overlay — azure.workload.identity/client-id: CLIENT_ID
    placeholder: "override-in-overlay"
